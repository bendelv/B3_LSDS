from flask import Flask


class Communication:
    def __init__(self, bootstrapAdd):
        "retrieve all connected adress nodes from bootstrap node"
        return pass

    def publish(self, jsonObj, users):
        "Send a json object to all connected user"
        return pass

    def listen(self):
        ""
        return pass
